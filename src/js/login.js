require(["/js/config.js"], () => {
    require(["header", "footer"], () => {
     class Register{
         constructor(){
             this.init()
         }
         init(){
             
         }
     }
    });
});
